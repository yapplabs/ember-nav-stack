{{#if shouldRenderMore}}
  {{to-nav-stack
      layer=0
      item=(component 'routable-components/yapp/more')
      header=(component 'routable-components/yapp/more/header')
  }}
{{/if}}

{{to-nav-stack
    layer=layerIndex
    item=(component routeComponent model=model controller=this)
    header=(component headerComponent model=model controller=this)
}}

{{outlet}}
